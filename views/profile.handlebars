
<div class="row">
  <div class="col-sm-4 offset-sm-4">
    <p>{{user.username}}</p>
    <p>{{user.email}}</p>
    <p>{{user.phone}}</p>

      <a href="/trips/new" class="btn btn-primary">New Trip</a>

      <a href="/comment" class="btn btn-primary">Leave a comment</a>
    </div>
  </div>
